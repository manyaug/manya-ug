/**
 * MANYA GLOBAL CONSTANTS
 * Centralized configuration for the entire application.
 */

export const ASSET_VERSION = 'v3.0.15';
export const REPO_OWNER = 'manyaug';
export const REPO_NAME = 'manya-react-assets';

export const CDN_BASE = `https://cdn.jsdelivr.net/gh/${REPO_OWNER}/${REPO_NAME}@${ASSET_VERSION}/`;
